if [[ $# -eq 0 ]]; then
    BUILD
    INSTALL
else
    case $1 in
        "--build" ) BUILD ;;
        "--install" ) INSTALL ;;
        "--clean" ) CLEAN ;;
        *) fatal "Unknown argument!"
    esac
fi
